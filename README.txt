Firt way, running withoun docker:
        To run app run in command line:
            >>> python manage.py runserver
        Then, go to:
            http://localhost:8000/weather_check/

Second, running with docker:
        Tape in command line:
            >>> docker-compose up
        Then, go to:
            http://localhost:8000/weather_check
